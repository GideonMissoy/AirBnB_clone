#!/usr/bin/python3
"""
City class
"""
from model.base_model import BaseModel

class City(BaseModel):
    """
    state_id(str) - state id
    name(str) - city name
    """

    state_id = ""
    name = ""
